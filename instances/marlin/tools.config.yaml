version: 1
signed_by: ""

tools:
  allowed:
    - name: read_file
      paths:
        - "/vault/"
    - name: write_file
      paths:
        - "/vault/Tasks/"
        - "/vault/Projects/"
        - "/vault/Daily/"
        - "/vault/Inbox.md"
        - "/vault/System/Memory/"
        - "/vault/Insights/"
        - "/vault/Learning/"
  denied: []

transfer:
  allowed: false
  max_size_kb: 1024
  employer_email: ""
