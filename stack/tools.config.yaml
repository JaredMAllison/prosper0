version: 1
profile: work
signed_by: ""

tools:
  allowed:
    - name: read_file
      paths:
        - "/vault/"
    - name: write_file
      paths:
        - "/vault/"
  denied: []

transfer:
  allowed: false
  max_size_kb: 1024
  employer_email: ""
